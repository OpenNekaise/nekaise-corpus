# Downloaded bytes — fetch your own copy with the loader; respect each source's license.
# These hold copyrighted content under mixed licenses and must NEVER be committed.
raw/
text/

# Agent scratch space — one-off scripts & notes stay local (see workspace/README.md)
workspace/*
!workspace/README.md

# Daily-dig / marathon run logs (scripts/dig.sh, scripts/marathon.sh)
logs/

# Python
__pycache__/
*.py[cod]
.venv/

# OS / editor
.DS_Store
.idea/
.vscode/
