# Raw bytes (sha256 in manifest.jsonl) are the reproducibility anchor and are version-independent.
# The EXTRACTED text in text/ is derived and can vary slightly across pypdf / beautifulsoup4
# versions — pin exact versions (==) if you need byte-identical text across machines.
requests>=2.31
pyyaml>=6.0
pypdf>=6.0,<7
beautifulsoup4>=4.12
